/*
  Track generation lands in build step 5, together with the fairness
  invariant and its solvability test. This file exists now so the seam
  between simulation and generation is fixed from the start.
*/

export function generateAhead() {
  return [];
}
